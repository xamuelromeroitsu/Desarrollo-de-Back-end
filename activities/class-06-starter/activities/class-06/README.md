# Actividades — Clase 06

Tu evidencia del taller vive aquí:

1. `work-log.md` — tu bitácora: se llena DURANTE el taller, no al final.
2. `validation-evidence.txt` — la salida final de `npm run validate:class-06`
   (sin secretos: el validador ya evita imprimirlos).

Commits sugeridos a lo largo del camino:

```text
class-06-environment-ready    (doctor en verde, seed comprobado)
class-06-regression-fixed     (BUG-106 resuelto + prueba de regresión)
class-06-submission           (FEATURE-206 + validador PASSED)
```

Tag final: `class-06-submission`. Nunca incluyas tu `.env`.
