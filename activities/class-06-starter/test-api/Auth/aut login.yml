info:
  name: "aut login "
  type: http

http:
  method: POST
  url: http://localhost:3000/auth/login
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
