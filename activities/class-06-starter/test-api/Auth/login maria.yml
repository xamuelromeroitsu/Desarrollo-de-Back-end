info:
  name: login maria
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/auth/login
  body:
    type: json
    data: |-
      {
        "email": "ana.requester.seed@example.test",
        "password": "maria clave del taller 06"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
