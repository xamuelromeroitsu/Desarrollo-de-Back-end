meta {
  name: Login Maria (agent)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "maria.agent.seed@example.test",
    "password": "maria clave del taller 06"
  }
}

script:post-response {
  const json = res.getBody();
  bru.setVar("token", json.accessToken);
}

// Prueba: login de un agente → 200 con Bearer.
// Maria puede ver TODAS las solicitudes y su historial (role agent).
// Si se corre este login después del de Ana, el token queda como el de Maria.