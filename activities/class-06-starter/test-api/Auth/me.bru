meta {
  name: Me
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/auth/me
  auth: bearer
  body: none
}

auth:bearer {
  token: {{token}}
}

// Prueba: el token identifica al usuario (200 con id, email y role, SIN passwordHash).
// Sin token → 401.