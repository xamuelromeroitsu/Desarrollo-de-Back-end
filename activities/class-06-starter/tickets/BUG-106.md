# BUG-106 — Empty filtered collection answers 404

Reported by: support team
Severity: medium
Status: open

## Symptom

When a valid collection filter has no matches, `GET /requests` returns `404`.

Steps to reproduce (with the workshop seed):

1. Log in as `ana.requester.seed@example.test`.
2. Request `GET /requests?status=closed` with her token.
3. Observe the response.

Ana has no closed requests, so the filter is valid and matches nothing.

## Expected behavior

A valid collection with no matches must return:

```http
200 OK
```

```json
[]
```

An empty collection is NOT a missing resource. `404` is for an individual
resource that does not exist (or must not be revealed) — see the class 3
contract for collections.

## Definition of done

1. `GET /requests?status=closed` (as Ana) answers `200` with `[]`.
2. The rest of the suite still passes.
3. A regression test pins the behavior so it cannot silently come back.




en la parte del bug al correr el comando

"test": "node --test --test-concurrency=1 'test/  aqui va el nombre del archivo.test.js'",

correr directo ahora

node --test --test-concurrency=1 test

al correr el comando se ve en rojo 

                 node --test --test-concurrency=1 test
node:internal/modules/cjs/loader:1520
  throw err;
  ^

Error: Cannot find module 'C:\Users\user\Desktop\Desarrollo-de-Back-end\activities\class-06-starter\test'
    at Module._resolveFilename (node:internal/modules/cjs/loader:1517:15)
    at wrapResolveFilename (node:internal/modules/cjs/loader:1071:27)
    at defaultResolveImplForCJSLoading (node:internal/modules/cjs/loader:1095:10)
    at resolveForCJSWithHooks (node:internal/modules/cjs/loader:1122:12)
    at Module._load (node:internal/modules/cjs/loader:1294:5)
    at wrapModuleLoad (node:internal/modules/cjs/loader:255:19)
    at Module.executeUserEntryPoint [as runMain] (node:internal/modules/run_main:154:5)
    at node:internal/main/run_main_module:33:47 {
  code: 'MODULE_NOT_FOUND',
  requireStack: []
}

Node.js v24.18.0
✖ test (90.6574ms)
ℹ tests 1
ℹ suites 0
ℹ pass 0
ℹ fail 1
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 102.3281

✖ failing tests:

test at test:1:1
✖ test (90.6574ms)
  'test failed'


  ahora se borra y se Aplicar el fix en requests.service.js — borrar las líneas 76-79 (if (rows.length === 0) { throw ... }), dejando return rows.map(mapRequestRow);.